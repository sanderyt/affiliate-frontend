import React, { FC } from "react";

import { StyledHeader } from "./styles";

interface Props {}

export const Header: FC<Props> = ({}) => {
  return <StyledHeader>Dit is de header</StyledHeader>;
};
