import { createMuiTheme } from "@material-ui/core/styles";

export const customTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#34ace0"
    },
    secondary: {
      main: "#227093"
    }
  }
});
